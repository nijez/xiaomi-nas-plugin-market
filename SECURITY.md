# 安全说明

仅用于已获授权的自有 NAS。首次安装需要 root SSH，插件市场本身有安装程序和写入插件服务配置的能力；请只安装你信任的发布包。

市场接口仅监听 NAS 回环地址，通过小米客户端的当前设备入口访问。修改请求要求会话和 CSRF 令牌。签名用于确认包来自相应密钥持有者，不等于代码天然安全；首次安装需要核对发布渠道、ZIP 校验值及公钥指纹。

不要将管理端口转发到公网。各插件的运行用户和授权边界不完全相同，详见其 README。root 或同一宿主账号的本机进程不在 WebDAV 权限隔离边界之外。

更新前备份重要文件和插件配置。共享开放后，拥有写权限的客户端可以修改文件。固件升级可能覆盖入口或服务，恢复前重新核对账号、目录及证书，不要为了修复访问而把权限改成 777。

请勿在公开 Issue 中提交可用凭据或私人文件。发现敏感信息泄漏时先吊销对应凭据，再联系仓库维护者；仅删除帖子不能撤销已经公开的秘密。

公开发行物不应包含真实配置、SSH 私钥、发布签名私钥、CA 私钥或个人身份数据。每次发布运行 `scripts/verify_release.py`，并人工复核新增文件。扫描通过不是全面渗透测试证明。
