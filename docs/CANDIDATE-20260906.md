# 2026-09-06 加固源码候选

分支：`codex/security-hardening-20260906`。用户授权继续开发并推送源码；不合并主分支、不创建 Release、不更新签名安装包、不操作在线 NAS。

## 提交范围

- Windows 命令传输修复，Windows/Mac 共用 NAS 激活流程。
- 安装互斥、服务状态回滚、异常时保留恢复材料。
- 私有代理通道与设备所有者证书鉴权，旧会话失效，无 loopback 免鉴权回退。
- 阿里云盘同大小不同内容识别、冲突保留和下载内容验证。
- 严格离线 wheel/哈希检查、缺依赖阻止构建；目前完整 ARM64 依赖仍未制备。
- 保留此前市场滚动、字号和窄窗口修复，附带自动化回归。
- 修正 README 与 Windows 说明中的未发布版本承诺；新增暂存区隐私检查，扫描实际 index blob，不扫描其未暂存替代文本。

没有加入本机截图、证书、令牌、私钥、真实 NAS 配置、账户文件、旧 ZIP 或新二进制制品。

## 本轮验证

Python 回归共 154 项通过，无跳过：公共模块及暂存区扫描 25、市场 24、115 10、阿里云盘 18、qB 34、WebDAV 33、设备管家 10。

市场 Playwright 布局套件通过：1 个测试包含 8 种模式/宽度组合，即 standalone 与模拟 micro-app 各自覆盖 1280、760、393、320px；验证滚动到最后一项、字号、横向边界、标签切换和图片加载。本轮没有操作真实 Mac/Android 客户端，因此这是浏览器夹具验收，不是实机验收。

三个 shell 安装器 `bash -n`、两个云盘前端 `node --check` 通过。PowerShell 参数往返夹具随市场回归通过，不等同 Windows PowerShell 5.1 实体安装。

提交前运行：

```bash
git diff --cached --check
python3 scripts/verify_release.py --staged
```

`--staged` 只做只读的暂存文件隐私模式检查，不读取发布私钥、不改制品、不输出命中的秘密值，也不代表签名包验收。模式扫描不能替代人工复核。

## 后续发布前置条件

1. 准备和审查完整 Linux ARM64 离线依赖，验证 Python/ABI 和实际 SDK 调用。
2. 完成 Mac/Android 局域网及 P2P 中继身份验证闭环；不能仅凭客户端 token 函数名判定身份可信。
3. 补齐持久化安装事务及断电恢复，继续进程与权限隔离审查。
4. 在独立测试设备完成 Windows 10/11、RP05、双端客户端验收，再另行授权签名、发布与部署。

源码推送不消除现有安装包或在线设备的风险；旧签名快照不包含本轮安全修复。
